<odoo>
    <data>
        <!-- 
            Este será el unico registro que tendra la tabla ya que es una pagina de 
            configuracion. 
        -->
        <record id="affiliation_configuration_default" model="affiliation.affiliation_configuration">
        </record>

        <record model="ir.ui.view" id="affiliation_affiliation_configuration_form">
            <field name="name">affiliation.affiliate_configuration.form</field>
            <field name="model">affiliation.affiliation_configuration</field>
            <field name="arch" type="xml">
                <form create="false" delete="false">
                    <sheet>
                        <h2>Affiliation's configuration</h2>
                        <group>
                            <!-- <field name="current_affiliation_number" string="Last affiliation number" readonly="1"/> -->
                            <field name="next_affiliation_number"/>
                            <field name="affiliation_start"/>
                            <field name="set_disaffiliation_date"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>
    </data>
</odoo>